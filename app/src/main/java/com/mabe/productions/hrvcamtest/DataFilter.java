package com.mabe.productions.hrvcamtest;

import java.util.ArrayList;

public class DataFilter {
/*

    public double[] bandPass(double lowPass, double highPass, Complex[] values){

        double [] freqArray = new double[values.length];
        for(int i = 0; i<values.length; i++){
            freqArray[i] = (double)i/(double)values.length;
        }

        ArrayList<Complex> filteredArr = new ArrayList<>();

        for(int i = 0; i < freqArray.length; i++){
            if(freqArray[i] < highPass && freqArray[i] > lowPass){
                filteredArr.add(values[i]);
            }
        }





    }

    */
}
